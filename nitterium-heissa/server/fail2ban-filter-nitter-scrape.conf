# Tweet-Detailseiten (/<user>/status/<id>) kosten je Aufruf einen X-API-Call.
# Greift erst bei uebermaessigem Abruf aus einer IP, normales Lesen bleibt frei.
[Definition]
failregex = ^<HOST> .*"(GET|HEAD|POST) [^"]*/status/[0-9]+
ignoreregex =
